{
  "_note": "Real agent output from churn CSV run 2026-06-03. Agent used total_rows/total_columns instead of schema-specified rows/columns. Kept as a standing fixture so the contract test catches this deviation on future runs.",
  "shape": {
    "total_rows": 100,
    "total_columns": 9,
    "target": "churned"
  },
  "columns": [
    {
      "name": "customer_id",
      "type": "text",
      "flags": ["high_cardinality", "id_like"],
      "summary": "Unique customer identifier for each record."
    },
    {
      "name": "churned",
      "type": "categorical",
      "flags": ["low_cardinality"],
      "summary": "Binary churn indicator that appears to be the outcome of interest."
    }
  ],
  "flags": ["small_dataset", "binary_target_present"]
}
